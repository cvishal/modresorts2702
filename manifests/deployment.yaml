# Generated by IBM TransformationAdvisor
# Thu Feb 28 06:00:20 UTC 2019

apiVersion: v1
kind: Service
metadata:
  name: retailtrader
  labels:
    app: retailtrader
spec:
  ports:
    - port: 9080
  selector:
    app: retailtrader
  type: NodePort

---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: retailtrader
  name: retailtrader
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: retailtrader
    spec:
      containers:
      - image: mycluster.icp:8500/default/retailtrader:latest
        name: retailtrader
        env:
          - name: WLP_LOGGING_CONSOLE_FORMAT
            value: json
          - name: WLP_LOGGING_CONSOLE_LOGLEVEL
            value: info
          - name: WLP_LOGGING_CONSOLE_SOURCE
            value: message,trace,accessLog,ffdc
        ports:
        - containerPort: 9080
          protocol: TCP
